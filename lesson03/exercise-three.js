let things = ['pencil', 'phone', 'scissors', 'towel', 'ruler', 'sketchbook'];

things.forEach(function(thing, index) {
  console.log(`${index+1},${thing}`);
  
  /*wasn't sure whether to start at base 0, but directions said to print out like so:
  
        "Example:

        // 1,cats
        // 2,dogs

        ..and so on"
    
   So I did as such */
});